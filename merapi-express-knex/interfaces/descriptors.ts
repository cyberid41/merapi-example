export interface IContactMap {
    name: string;
    address: string;
    phone: string;
    email: string;
}

export interface IContactDescriptor {
    id?: string;
    name: string;
    address: string;
    phone: string;
    email: string;
}